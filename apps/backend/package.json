{
  "name": "@app/backend",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "sst dev",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove",
    "console": "sst console",
    "db:start": "pnpm db:get-url 'pnpm dlx prisma db push && pnpm dlx prisma studio --browser none'",
    "db:push": "pnpm db:get-url 'pnpm dlx prisma db push'",
    "db:studio": "pnpm db:get-url 'pnpm dlx prisma db push && pnpm dlx prisma studio'",
    "db:generate": "pnpm dlx prisma generate",
    "lint": "eslint . --ext .ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@lib/eslint": "workspace:*",
    "@lib/tsconfig": "workspace:*",
    "@types/node": "^20"
  },
  "dependencies": {
    "@prisma/client": "^6.7.0",
    "@trpc/server": "^11.1.2",
    "prisma": "^6.7.0",
    "sst": "3.14.12",
    "superjson": "^2.2.2",
    "zod": "^3.24.2"
  }
}
